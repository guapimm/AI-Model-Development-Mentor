# 开发工作流规范

## 一、项目初始化与文档体系

### 轻量模式（代码量 < 500 行）
仅需 `README.md`，包含：项目简介、技术栈、核心表结构、接口清单、部署步骤。

### 常规模式（代码量 ≥ 500 行）
项目启动时创建以下文档结构：

```
📁 /docs/
├── architecture.md      # 技术栈选型理由（生活化类比）、系统架构图（Mermaid）、目录结构
├── dev_log.md           # 开发日志：时间、变更、测试结果、已知问题及解决方案
├── api_interface.md     # 前后端接口契约（URL、参数、返回值、异常场景）
└── SNAPSHOT.md          # 核心快照（≤200行）：技术栈版本、表名清单、API路径、业务流程图
```

Token 优化：每次对话结束生成【上下文摘要】（进度、变量名、待办、续传暗号）。

## 二、前端可视化定位协议

编写前端代码前，先输出以下定位信息：

### 1. 页面布局图
使用 ASCII 线框图或 Mermaid 组件树明确页面结构。

### 2. UI 元素映射表

| 视觉位置 | 组件名称 | 对应文件路径 | CSS 类名/ID | 功能描述 |
|---------|---------|------------|------------|---------|
| 顶部导航栏右侧 | UserAvatar | /src/components/Header.tsx | .user-avatar | 用户头像及下拉菜单 |

### 3. 前端事件映射表

| 中文名 | 操作 | 调用后端接口 | 预期效果 |
|-------|------|------------|---------|
| 登录按钮 | 点击 | POST /api/login | 跳转首页，存储 Token |

## 三、部署与灾备机制

### 本地备份
- 提供一键备份脚本 `backup.sh`，将代码+配置+数据库导出至 `./local_backup/`
- 每次部署前检查本地备份是否存在，否则拒绝部署

### 云服务器灰度回滚
- 部署新代码前，自动将旧版本压缩为 `backup_时间戳.zip`
- 紧急回滚三步操作：
  1. `./rollback.sh latest` — 解压最新备份
  2. `docker-compose restart`（或 `pm2 restart all`）
  3. `./health_check.sh` — 输出服务状态
- 在 `dev_log.md` 中记录备份时间、路径及回滚记录

### 环境隔离
- 区分开发/生产环境配置
- 提前提示生产环境需修改的安全配置项

## 四、需求发散与建议

完成用户指定功能后，输出《功能增强建议卡》：

- ✅ **已完成功能总结** — 清晰说明当前可用功能
- 🔮 **潜在风险预警** — 并发访问、数据一致性、第三方依赖等
- 🚀 **推荐扩展功能** — 标注优先级 P0/P1/P2、实现难度⭐级、预期效果
- ⚠️ **小白避坑指南** — 常见误区、操作注意事项

## 五、测试与自检闭环

### 最小可验证测试用例
提供用户可手动操作的验证步骤，例如：
> "点击登录按钮，输入正确账号密码，验证是否成功跳转首页"

### 逻辑自洽性声明
代码输出后必须声明：
> "我已检查：①变量作用域正确 ②异步处理完整 ③异常捕获覆盖 ④无敏感信息泄露 ⑤无明显性能瓶颈"

## 六、版本锚点

每完成一个里程碑，输出规范 Git Commit 信息：
```
feat: 用户登录模块完成
- 实现 JWT Token 认证
- 添加密码哈希存储
- 前端登录表单校验
Author: AI Assistant
Date: 2026-08-08
```
